<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="App1.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="IMBD for Drinks - Admin">

    <!--<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Button x:Name="myButton" Click="myButton_Click">Click Me</Button>
    </StackPanel>-->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row ="0" Grid.ColumnSpan ="3" Background="#1f3d7a"/>
        <Border Grid.Row="1" Grid.Column="0" Background="#152951"/>
        <Border Grid.Row="1" Grid.Column="1" Background="Beige"/>
        <Border Grid.Row="1" Grid.Column="2" Background="GhostWhite"/>

        <!--<StackPanel Grid.Row ="1" Grid.Column="0" Margin="40,0,0,0" VerticalAlignment="Center">
            <TextBlock Foreground="White" FontSize="25" Text="Today - 64° F"/>
            <TextBlock Foreground="White" FontSize="25" Text="Partially Cloudy"/>
            <TextBlock Foreground="White" FontSize="25" Text="Precipitation: 25%"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal"
            HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Foreground="White" FontSize="25" Text="High: 66°" Margin="0,0,20,0"/>
            <TextBlock Foreground="White" FontSize="25" Text="Low: 43°" Margin="0,0,20,0"/>
            <TextBlock Foreground="White" FontSize="25" Text="Feels like: 63°"/>
        </StackPanel>-->

        <!--<StackPanel VerticalAlignment="Center" HorizontalAlignment="Left">
            <Image Height="Auto" Margin="20,0,0,0" Source="Assets/beers-logo.png"/>
        </StackPanel>-->

        <Image Margin="20,0,0,0" HorizontalAlignment="Left" Source="Assets/beers-logo.png"/>

        <Grid Grid.Row="1" Margin="20" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Search Box -->
                <RowDefinition Height="*"/>
                <!-- ListView (Expands) -->
                <RowDefinition Height="Auto"/>
                <!-- Button -->
            </Grid.RowDefinitions>

            <!-- Search Box -->
            <TextBox x:Name="SearchTextBox" Margin="20" Width="200" PlaceholderText="Search items..." Grid.Row="0"/>

            <!-- Scrollable ListView -->
            <ListView x:Name="ReviewsList" Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Padding="10" Background="LightGray" CornerRadius="5">
                            <StackPanel>
                                <TextBlock Text="{Binding content}" FontSize="14" Foreground="Black"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Button stays fixed -->
            <Button Content="Automatically check reviews" Margin="20" Grid.Row="2"/>
        </Grid>

    </Grid>

</Window>
